services:
  - type: web
    name: titan-ipsec-platform
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0: --workers 2 --timeout 120 backend.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
