version: '3.8'

services:
  titan-analyzer:
    build: .
    container_name: titan-ipsec-platform
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - FLASK_ENV=production
    volumes:
      - ./reports:/app/reports
      - ./dataset:/app/dataset
